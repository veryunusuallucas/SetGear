# Normaliza fim de linha no repositório (LF), independente do sistema de quem clona.
# Sem isto, o mesmo arquivo aparece como alterado ao trocar de máquina.
* text=auto

# Binários — nunca normalizar.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
